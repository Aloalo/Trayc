#include "UserSettings.h"

UserSettings::UserSettings(void) :
    SETTING(screenWidth),
    SETTING(screenHeight),
    SETTING(FOV),
    SETTING(drawDistance),
    SETTING(ctVertices),
    SETTING(Lstep),
    SETTING(minX),
    SETTING(maxX),
    SETTING(minY),
    SETTING(maxY)
{
}

