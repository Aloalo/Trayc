#include "UserSettings.h"

UserSettings::UserSettings(void) :
    SETTING(screenWidth),
    SETTING(screenHeight),
    SETTING(FOV),
    SETTING(ctVertices),
    SETTING(minX),
    SETTING(maxX),
    SETTING(minY),
    SETTING(maxY)
{
}

