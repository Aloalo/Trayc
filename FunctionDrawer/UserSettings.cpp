#include "UserSettings.h"

UserSettings::UserSettings(void) :
    SETTING(screenWidth),
    SETTING(screenHeight),
    SETTING(FOV),
    SETTING(ctVertices),
    SETTING(scaleX),
    SETTING(scaleY),
    SETTING(offsetX),
    SETTING(offsetY),
    SETTING(useOptimization),
    SETTING(smallData)
{
}

