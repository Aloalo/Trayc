Opcenito:
    -AntTweakLib update
    -update optix and cuda (optix release)
    -stl extensions (arraysize) (optix release)
    
Grafika:
-area lights (rectangle)
-mipmaping (optix release)
-glass/reflective mesh textures
-parallax mapping

-distributed ray tracing
	-motion blur
	-diffuse reflections
	-fuzzy transparency
-global illumination
	-(bidirectional) path tracing
	-photon mapping
