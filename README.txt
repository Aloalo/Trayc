Real time raytracing library using OptiX library.

How to install:
    Libraries:
        -assimp 3.1 ASSIMP_HOME (Tester only)
        -AntTweakBar ANTTWEAKBAR_HOME (Teter only)
        -DevIL (put in path)
        -CUDA 5.5+
        -OptiX 3.5.1+
        -SDL2 SDL2DIR
        -glew GLEW_HOME
